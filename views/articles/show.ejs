<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/style.css">
    <title><%= article.title %> - Owl Blog</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/"><img src="/owl3.png" alt="Owl Blog"></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/about">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact">Contact</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="article-content">
        <h1 class="mb-1"><%= article.title %></h1>
        <div class="meta-row">
            <%= article.createdAt.toLocaleDateString() %> by <%= article.author %>
            <% if (article.readingTime) { %> | <%= article.readingTime %> min read <% } %>
        </div>
        <% if (article.category) { %>
            <span class="chip"><%= article.category %></span>
        <% } %>
        <% if (article.tags && article.tags.length > 0) { %>
            <% article.tags.forEach(tag => { %>
                <span class="chip"><%= tag %></span>
            <% }) %>
        <% } %>
        <div class="my-3">
            <a href="/" class="btn btn-secondary">All Articles</a>
            <a href="/articles/edit/<%= article.id %>" class="btn btn-info">Edit</a>
        </div>

        <div class="article-prose">
            <%- article.sanitizedHtml %>
        </div>
        
    </div>
        <footer class="bg-dark text-white text-center py-3 mt-5">
                <p>&copy; <span id="year"></span> Owl Blog. All rights reserved.</p>
        </footer>
                <style>
                .copy-btn {
                    background: none;
                    border: none;
                    position: absolute;
                    top: 8px;
                    right: 8px;
                    padding: 0;
                    cursor: pointer;
                    opacity: 0.5;
                    transition: opacity 0.2s;
                }
                .copy-btn:hover {
                    opacity: 1;
                }
                .copy-btn img {
                    width: 22px;
                    height: 22px;
                }
                .copy-btn[data-tooltip]:hover::after,
                .copy-btn[data-tooltip].show-tooltip::after {
                    content: attr(data-tooltip);
                    position: fixed;
                    top: calc(var(--copy-tooltip-top, 0px));
                    left: calc(var(--copy-tooltip-left, 0px));
                    background: #222;
                    color: #fff;
                    padding: 6px 16px;
                    border-radius: 8px;
                    font-size: 16px;
                    font-weight: bold;
                    box-shadow: 0 2px 12px rgba(0,0,0,0.25);
                    z-index: 9999;
                    white-space: nowrap;
                    opacity: 1;
                    transition: opacity 0.2s;
                    pointer-events: none;
                }
                pre {
                    position: relative;
                }
                </style>
                <script>
                document.getElementById('year').textContent = new Date().getFullYear();
                document.addEventListener('DOMContentLoaded', function() {
                    document.querySelectorAll('pre > code').forEach(function(codeBlock) {
                        var pre = codeBlock.parentElement;
                        var button = document.createElement('button');
                        button.className = 'copy-btn';
                        button.setAttribute('data-tooltip', 'Copy');
                        button.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" aria-hidden="true" focusable="false"><rect x="9" y="9" width="11" height="13" rx="2" ry="2" fill="none" stroke="currentColor" stroke-width="2"/><rect x="4" y="4" width="11" height="13" rx="2" ry="2" fill="none" stroke="currentColor" stroke-width="2"/></svg>';
                        button.onclick = function(e) {
                            var text = codeBlock.innerText;
                            navigator.clipboard.writeText(text).then(function() {
                                // Calculate position for tooltip
                                var rect = button.getBoundingClientRect();
                                var tooltipTop = rect.top - 40;
                                var tooltipLeft = rect.left + rect.width / 2;
                                button.setAttribute('data-tooltip', 'Copied!');
                                button.classList.add('show-tooltip');
                                button.style.setProperty('--copy-tooltip-top', tooltipTop + 'px');
                                button.style.setProperty('--copy-tooltip-left', tooltipLeft + 'px');
                                setTimeout(function() {
                                    button.setAttribute('data-tooltip', 'Copy');
                                    button.classList.remove('show-tooltip');
                                }, 1500);
                            });
                        };
                        pre.style.position = 'relative';
                        pre.appendChild(button);
                    });
                });
                </script>
</body>
</html>